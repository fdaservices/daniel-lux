# This is NOT the Next.js you know

This project uses Next.js 16 (App Router). It has breaking changes — APIs,
conventions and file structure may differ from older training data. Read the
relevant guide in `node_modules/next/dist/docs/` before writing any code, and
heed deprecation notices.
